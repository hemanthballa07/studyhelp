/**
 * Integrity context — exam/live-assessment prompt detection driving pedagogical mode-switching,
 * plus online judge sampling of delivered answers. Cross-context effects flow via domain events
 * (outbox + dispatcher).
 */
package com.platform.integrity;
