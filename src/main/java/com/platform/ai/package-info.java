/**
 * AI context — grounded answer plus verification: corpus ingestion, hybrid retrieval + rerank,
 * citation-constrained generation, the 4-signal verifier, and the confidence/abstention
 * decision. Cross-context effects flow via domain events (outbox + dispatcher).
 */
package com.platform.ai;
